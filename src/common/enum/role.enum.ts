export enum Role {
  Admin = 'admin',
  User = 'user',
}

export enum SubmitType {
  NewShop = 0,
  NewOperating = 1,
  NewProduct = 2,
}

export enum SubmitStatus {
  Pending = 0,
  Approved = 1,
  Rejected = 2,
}

export enum UsingType {
  Using = 0,
  Verifying = 1,
}
